%h1.title JSON Visited Locations

= form_tag visited_path, multipart: true do

  = label :radius, 'Radius'
  = text_field_tag :radius

  = label :route, 'JSON file'
  = file_field_tag :route, :accept => '.json', :class => 'upload'

  %p
  = submit_tag 'Get Locations'

%input#locations{type: 'hidden', value: @locations_json}
-unless @locations == nil
  %table#table
    %thead
      %tr
        %th Name
        %th Latitude
        %th Longitude
        %th Description
    %tbody
      - @locations.each do |location|
        %tr
          %td= location.name
          %td= location.latitude
          %td= location.longitude
          %td= location.description

%p
= link_to 'Back to location list', locations_path
